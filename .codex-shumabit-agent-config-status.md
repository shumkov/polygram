STATUS: DONE
WHAT: The initial review finding required a narrow Ivan-DM discovery scope and an exact pinned Codex 0.145 disposable probe; later reviewed revisions and the final validation receipt resolved it.
NEED: Nothing; superseded details are resolved by the final evidence below.
NEXT: Retained as historical review context.

STATUS: DONE
WHAT: Revised spec only; removed CLAUDE.md and .codex/skills proposals, excluded broad parity/memory/runtime rollout, and added exact closure plus rollback gates.
NEED: Nothing from this lane until the parent authorizes the next pipeline stage.
NEXT: Waiting.

STATUS: PROGRESS
WHAT: Folded final review corrections into a native-discovery-only MVP: root AGENTS plus one tracked .agents/skills directory link to canonical skills.
NEED: Nothing.
NEXT: Final consistency check only; no implementation.

STATUS: DONE
WHAT: Final spec now uses isolated exact-0.145 positive/negative Git probes, reconciled 50/48/47/1 catalog facts, discovery-only skill scope, and simple Git rollback.
NEED: Ivan/parent alignment before any source change; separate user approval for every action-skill behavior test.
NEXT: Waiting.

STATUS: DONE
WHAT: The first pinned-0.145 probe and its non-identical follow-up remain superseded, but the corrected `$codex-dir-link-control` A/B passed at `/tmp/codex-shumabit-validation.R1Qt1w`. Both cells share base `4486635`; only positive adds mode-`120000` `.agents/skills -> ../skills`; nonce polarity passed; and both JSONL item streams had zero command or other non-message items.
NEED: Nothing; corrected native-discovery evidence is verified.
NEXT: Gate PASS; use only the final artifact receipt and manifest recorded below.

STATUS: DONE
WHAT: Final implementation review found evidence drift only: origin/main advanced from inspected `5142c07` to implementation base `3d309437`, adding the valid `salary-calc` skill. Frozen catalog evidence is now 50 directories, 48 `SKILL.md` files, and 47 valid skills.
NEED: The exception set remains `xero-reconciliation` plus support directories `gog-reauth` and `xero-template-builder`; design and scope are unchanged.
NEXT: Continue implementation review against the corrected frozen evidence.

STATUS: DONE
WHAT: Review found stale root `AGENTS.md` exception text: `barter-sync` and `partner-inventory` have valid name-matching frontmatter and are loadable skills, while also serving as fixed-path cron payloads.
NEED: The reviewed root `AGENTS.md` change now permits only that factual cleanup; the exception set remains `gog-reauth`, `xero-template-builder`, and intentionally frontmatter-free `xero-reconciliation`.
NEXT: Continue implementation review with design, scope, cron paths, code, and deployment otherwise unchanged.

STATUS: DONE
WHAT: Final validation controls PASS at `/tmp/codex-shumabit-validation.R1Qt1w`: the corrected A/B satisfied shared-base/link-only and nonce-polarity predicates with zero command/non-message JSONL items; the native `$monthly-ops` prompt excluded the oracle and matched the independently extracted 148-character body sentence after trailing-newline normalization.
NEED: None. The artifact hash manifest SHA-256 is `e20a41b0965c67126602f86f35e9cc5329e83dbd88cf336ba1ba6bb1621d65ca`.
NEXT: Review-ready; implementation complete. No source, commit, or deployment action occurred in this documentation correction.
