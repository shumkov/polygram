# 0.11.0 — Channels driver review follow-up

Tracking the findings from the 2026-05-24 multi-agent ce-code-review pass and their resolution status.

**Review scope:** 10 reviewers (correctness, security, reliability, api-contract, adversarial, testing, maintainability, project-standards, agent-native, learnings-researcher) against the channels work since `0.10.0-spike`. ~70 raw findings → 19 actionable after dedup + anchor-75 gate.

**Run ID:** `20260524-205934-c35ff84c` (per-agent JSON artifacts at `/tmp/compound-engineering/ce-code-review/20260524-205934-c35ff84c/`).

## Status legend

- ✅ Fixed in this branch
- 🟡 Partially fixed (subset landed)
- ⏳ In progress
- ⬜ Not started

## P0 — must fix before production rollout

| # | Title | File | Status | Commit |
|---|---|---|---|---|
| 1 | Bridge secret leak via `--mcp-config` argv (`POLYGRAM_SOCK_SECRET` + `POLYGRAM_SESSION_KEY` visible in `ps`/`/proc/cmdline`) | `lib/process/channels-process.js:193` | ✅ | mcp-config written to 0o600 tmp file; argv carries file path only |
| 2 | File exfiltration via `reply.files` — prompt-injected Claude uploads `/etc/passwd`, SSH keys, AWS creds to TG | `lib/process/channels-tool-dispatcher.js:87` | ✅ | `validateAttachmentPath` + per-session allowlist + realpath; symlinks to outside rejected |
| 3 | `bridge-disconnected` event has zero consumers — daemon-respawn premise unimplemented | `lib/process/channels-process.js:310-319` | ✅ | ProcessManager listens → kills dead instance → frees LRU slot → lazy respawn via getOrSpawn on next msg (with persisted claudeSessionId for `claude --resume`) |

## P1 — must fix before Phase 1 default-flip

| # | Title | File | Status | Commit |
|---|---|---|---|---|
| 4 | Reply fan-out across pending turns — bridge `tool` msg carries no `turn_id` (user_msg does) | `lib/process/channels-process.js:413-420` + bridge protocol | ✅ | reply tool schema now requires `turn_id`; `_recordReplyForPendingTurn` routes by it; fallback for single-pending logged |
| 5 | hardTimer leak on bridge-disconnect (10-min zombie timers) | `channels-process.js:310-319` | ✅ | `c14f112` |
| 6 | Daemon-side ping loop never verifies pongs return — half-open socket invisible | `channels-process.js:370-372, 601-606` | ✅ | `lastPongAt` recorded on each `pong`; `pongWatchdog` fires every 5s; declares bridge dead at 30s |
| 7 | `tool_ack` 30s timeout returns "delivery failed" to Claude → may retry → duplicate TG msgs | `channels-bridge.mjs` | ✅ | daemon-side `recentToolCallIds` FIFO cache; duplicate `tool_call_id` re-ACKs without re-dispatch |
| 8 | `respondToPermission` blindly writes verdicts — double-fire writes two | `channels-process.js:363` | ✅ | `c14f112` (Set-based dedup) |
| 9 | Socket file permissions TOCTOU race (listen → chmod window) | `channels-process.js:152` | ✅ | `process.umask(0o077)` wraps `listen()`; restored immediately after |
| 10 | Inbound meta values (user/chat_id/msg_id/turn_id) not XML-escaped before `<channel ...>` attributes | `channels-bridge.mjs:135` | ✅ | `c14f112` (`escapeChannelAttr`) |
| 11 | Channels tmux name lacks `polygram-` prefix → orphan-sweep blind → leaks per restart | `channels-process.js:134` | ✅ | `c14f112` |
| 12 | Quiet-window heuristic resets forever when Claude streams progress replies → 10min hang | `channels-process.js:417` | ✅ | `maxRepliesPerTurn` cap (default 20); resolves immediately on cap hit |
| 13 | `approval-required.toolInput` is object `{description, input_preview}` but TmuxProcess emits string; `normalizeTuiToolInput` coerces non-string → `''` → silent empty approval card | `channels-process.js:352-356` | ✅ | `_formatToolInputForApproval` returns string; folds description into preview when distinct |
| 14 | `pendingQueue` never populated → no Telegram live-edit, reactor heartbeats, subagent announce on channels chats | `channels-process.js:454-486` | ✅ | `send()` pushes `{turnId, context}` to `pendingQueue`; `_resolveTurn` + drain paths pop it |
| 15 | Missing canonical events: `stream-chunk`, `autonomous-assistant-message`, `compact-boundary`, `tool-use`, `phase-change` | `channels-process.js:141-553` | 🟡 | `tool-use` + `autonomous-assistant-message` emitted; `stream-chunk`/`compact-boundary`/`phase-change` deferred (channels protocol doesn't expose) |
| 16 | Reply text recorded even when `toolDispatcher` failed | `channels-process.js:408` | ✅ | `c14f112` |
| 17 | `HeartbeatReactor.inStall` never cleared on liveness → 🥱 frozen after first stall | `heartbeat-reactor.js:179` | ✅ | `c14f112` |
| 18 | `_handleStartupDialogs` trust/dev-channel/timeout branches untested | `channels-process.js:215-253` | ✅ | 3 new integration tests cover dev-channel Enter, trust-dialog Enter, timeout-throw. Bonus: caught + fixed a race in `_waitForBridgeHandshake` (early bridge-ready event was lost). |
| 19 | `channels-bridge.mjs` has zero direct tests | `channels-bridge.mjs:1-246` | ✅ | New `tests/channels-bridge.test.js` spawns the real bridge subprocess against a fake daemon socket. 6 tests: env validation, hello+session_init, stdin EOF clean exit, hello_reject exit code, ping→pong, hostile-input safety. |

## P2 — should address (15 condensed)

| Category | Items | Status |
|---|---|---|
| Cleanup leaks on startup paths | R6+R7+R10 (3 throw paths leak tmux+socket) | ✅ `c14f112` (try/catch in start) |
| respond closure drops deny-with-feedback message | AC2 | ⬜ accepted — Channels protocol doesn't carry feedback string |
| `pickBackend` silent typo fall-through | AC3 | ✅ `c14f112` |
| cost=0 + zero metrics propagate to billing | AC4 | ✅ PmSendResult now uses `null` to mean unmeasured-subscription |
| C28 fireUserMessage skip breaks `/compact` silent-retry | AC7 | ✅ implemented `fireUserMessage` (writes user_msg w/o pending-turn) |
| C13 resetSession skip breaks `/new`+`/reset` | AC8 | ✅ implemented `resetSession` (drains pendings, clears claudeSessionId, emits session-reset) |
| Stale ping resets watchdog | R5 | ✅ bridge only updates `lastPing` on actual `ping`, not any inbound data |
| Partial file-attach silent ok:true | R9 | ✅ rolled into P0 #2 — dispatcher returns ok:false with details on failed attaches |
| ChannelsProcess 600+ LoC owns 5 concerns | M1 | ✅ `ChannelsBridgeServer` (~200 LoC: socket lifecycle + auth + schema validation) + `runStartupGate` helper (`lib/tmux/startup-gate.js`, `ee5238f`: capture/sendKeys/timeout polling loop with configurable triggers + ready signal + timeout code) both extracted |
| `resolveClaudeBin` dead helper | M2 | ✅ `c14f112` |
| `deliverReplies` two code paths | M3 | ✅ required at construction; no lazy fallback |
| No `pendingPerms` map (daemon restart loses) | ADV-12 | ⬜ deferred — needs DB-level persistence of in-flight perm_reqs across daemon restart. Same shape as `lib/approvals/store.js` but with channels-specific schema. Tracked in `docs/0.11.0-channels-backend-divergence.md`. |
| No rate limit on reply tool calls | ADV-6 | ✅ token-bucket (5/s, burst 20) on `_dispatchToolCall` |
| `chunkTextPlain` splits HTML mid-tag | ADV-14 | ✅ polygram.js uses `chunkMarkdownText` (fence-aware) for channels driver |
| Outbound reply.text not escaped (defense-in-depth) | ADV-10 | ⬜ accepted — no real attack vector inside daemon process |
| `claudeSessionId` adopt visible to no upstream cache | ADV-8 | ✅ already resolved — ChannelsProcess.start emits `init` AFTER bridge handshake adopts canonical claude_session_id; existing `sdkCallbacks.onInit` calls `db.upsertSession` with the adopted id (same path SDK/tmux use). Bridge sending a different session_id only happens at handshake time, so no mid-run refresh wiring needed. |
| Bridge protocol no zod schemas | M4 | ✅ `lib/process/channels-bridge-protocol.js` — zod schemas for every msg kind, used by `ChannelsBridgeServer` to safeParse inbound on the daemon side |
| Magic numbers scattered | M5 | 🟡 named most ping/watchdog/burst constants in P1 batch; full audit deferred |

## P3 — narrow scope

- Reaction whitelist 🤖 may not be in Bot API allowlist — wrapped in try/catch, advisory — ✅ accepted (already swallow on error)
- `--dangerously-load-development-channels` kill-switch missing — ✅ documented in risk register
- `chat_id`-mismatch log no rate limit — ✅ 1s throttle per session
- `/tmp` socket files survive `kill -9` — 🟡 partial via orphan-sweep (channels prefix landed in P1 #11); full sweep planned in daemon-wiring round
- HeartbeatReactor stop() races in-flight setReaction — ✅ monotonic `_reactionToken`
- `interrupt()` doesn't clear pending turn timers — ✅ 5s grace window → `resultSubtype: 'interrupted'` resolution
- Naming drift `claudeSessionId` / `CLAUDE_SID` — ✅ bridge renamed `CLAUDE_SID` → `CLAUDE_SESSION_ID`
- `phase0-bridge.mjs` spike code orphaned — ✅ deleted `scripts/channels-spike/`
- `simulateCompactBoundary` throws instead of returning sentinel — ✅ returns `{supported:false, reason}`
- Test fake-runner duplicated (M6) — ⬜ accepted; channels runner shape differs from tmux runner, full dedup low-value
- `polygram.js` wiring inline (watch on backend #4) — ⬜ watch-item, no action

## Cross-backend asymmetry — ✅ `docs/0.11.0-channels-backend-divergence.md`

Companion to `docs/0.10.0-backend-divergence.md`. Documents:
- Event surface delta (`stream-chunk`, `compact-boundary`, `phase-change` accepted-N/A; rest matched)
- `PmSendResult` shape delta (cost: null, token metrics: null — unmeasured-subscription)
- Method matrix (resetSession/fireUserMessage matched in 0.11.0 P2 batch; injectUserMessage/steer/setModel/setPermissionMode deferred)
- pendingQueue context (P1 #14 — channels now matches)
- Bedrock/Vertex/Foundry incompatibility
- Operator decision matrix (when to pick which backend)

## Learnings to honor (from `ce-learnings-researcher`)

- **rc.32 STALL=45s** calibrated against shumorobot Music traffic — HeartbeatReactor reuses correctly
- **rc.39/rc.45 sanitize-reply** — verify `lib/telegram/sanitize-reply.js` runs on channels-tool-dispatcher path
- **S1 spawn-leak pattern** — `ChannelsProcess.start` now has try/catch teardown per `c14f112`
- **rc.33 onInit topic-config resolution** — verify per-topic `pm:'channels'` override honored
- **lib/ipc/server.js** is the canonical mirror pattern (stale-socket unlink + 0600 + timingSafeEqual)

## Resolution rollout plan

Working through tiers in order: **P0 → P1 → P2 → P3**.

- Phase 0 (now): P0 #1, #2, #3 (the daemon-respawn half remaining)
- Phase 1 (next): P1 items #4, #6, #7, #9, #13, #14, #15
- Phase 2: P1 test gaps #18, #19
- Phase 3: P2 batch
- Phase 4: P3 + cross-backend divergence doc

Each commit updates this doc's status column.

---

## Parity audit — 2026-05-25 (post-review-followup)

Second-pass review: systematic SdkProcess + TmuxProcess walk asking "does ChannelsProcess have the equivalent?". 26 findings. Two P0s missed by the first multi-agent review.

| # | Category | Where (sdk/tmux ref) | Gap | Severity | Status |
|---|---|---|---|---|---|
| P1 | Constructor | sdk-process.js:179 + logEvent sites | No `db` option / zero `_logEvent` rows — channels invisible in ops dashboards | P1 | ✅ `725d05e` |
| P2 | Constructor | sdk-process.js:181, tmux-process.js:355 | No `queueCap` (unbounded pendingTurns + pendingQueue) | P1 | ✅ `725d05e` |
| P3 | Spawn args | tmux-process.js:589 (POLYGRAM_DISPLAY_HINT) | Missing `--append-system-prompt POLYGRAM_DISPLAY_HINT` — shumorobot 2026-05-15 incident reproducible | P1 | ✅ `725d05e` |
| P4 | Spawn args | tmux-process.js:527 (--agent) | Missing `--agent` — per-chat agents silently don't load | **P0** | ✅ `9f39ac0` |
| P5 | Spawn args | tmux-process.js:520 (--effort) | Missing `--effort` flag passthrough | P1 | ✅ `9f39ac0` |
| P6 | Spawn args | tmux-process.js:502-505 (isolateUserConfig) | Channels always --strict-mcp-config but never --setting-sources; inconsistent | P1 | ✅ `725d05e` |
| P7 | Spawn opts | tmux-process.js:485-488 (getTopicConfig) | No topicConfig precedence — per-topic model/effort/cwd ignored | P1 | ✅ `9f39ac0` |
| P8 | Spawn args | tmux-process.js:514-518 (--resume vs --session-id) | Lazy respawn uses `--session-id <existing>` which **creates new session**, not resume → conversation history lost | **P0** | ✅ `9f39ac0` |
| P9 | Spawn | tmux-process.js:632-654 (sessionExists reconcile) | No pre-spawn `sessionExists` check (random suffix makes collisions astronomical) | P3 | ✅ accepted |
| P10 | Spawn | tmux-process.js:600 (verifyPinnedClaudeBin) | Channels doesn't re-verify (factory pre-verifies; OK) | P2 | ✅ accepted |
| P11 | Spawn | tmux-process.js:563 (--settings hooks) | No hook ndjson — no `hook-event` rows in db | P2 | ✅ doc-as-divergent (divergence doc) |
| P12 | Spawn | tmux-process.js:526 (--debug-file) | Internal tool-use events (Bash/Write/Edit) not visible — only bridge tools | P2 | ✅ doc-as-divergent |
| P13 | Send | sdk-process.js:48 / tmux-process.js:862 (control-char sanitize) | No control-char strip on prompt | P3 | ✅ doc-as-divergent |
| P14 | Event | sdk-process.js:594 / tmux-process.js:3176 | Missing `queue-drop` emit (couples with P2) | P1 | ✅ `725d05e` |
| P15 | Event | sdk-process.js:532 / tmux-process.js:930 | Missing `turn-timeout` emit (channels rejects but doesn't emit) | P1 | ✅ `725d05e` |
| P16 | Event | sdk-process.js:733 (inject-user-message), tmux:3231 | Missing emits for deferred methods (events even if methods deferred) | P3 | ✅ doc-as-divergent |
| P17 | Event shape | init payload | Channels emits `{sessionId}` (camel), tmux emits `{session_id, label, backend, tmux_name}` (snake) | P2 | ✅ `2bbef0a` |
| P18 | Event shape | result payload | Channels omits `resolvedVia` + `stopReason` from second arg | P2 | ✅ doc-as-divergent |
| P19 | Lifecycle | tmux-process.js:3254 (_killing flag) | `kill()` has no in-flight re-entry guard; concurrent kills race | P2 | ✅ `2bbef0a` |
| P20 | Lifecycle | tmux-process.js drainQueue | kill() inline-drains instead of calling `this.drainQueue('KILLED')` | P3 | ⬜ accepted — channels drain handles pendingTurns separately from pendingQueue, refactor low-value |
| P21 | Error code | tmux-process.js:775 (PROCESS_CLOSED) | `send()` after kill throws plain Error, no `code: 'PROCESS_CLOSED'` | P2 | ✅ `725d05e` |
| P22 | Error code | channels-process.js:418 | Bridge handshake / dialog timeouts have no `code` | P2 | ✅ `2bbef0a` |
| P23 | Event | sdk-process.js:347 (assistant-message-start) | No new-bubble boundary signal (channels concatenates replies with \n\n) | P3 | ✅ doc-as-divergent |
| P24 | Security | channels-process.js:547 | chat_id-only validation (thread_id is session-bound, safe by construction) | P3 | ✅ accepted |
| P25 | Permission | tmux-process.js:2901 ('always-allow') | `respondToPermission` accepts only allow/deny; no always-allow | P2 | ✅ doc-as-divergent |
| P26 | Send | sdk-process.js:526 (context.onActivate/onFirstStream) | Channels doesn't call onActivate/onFirstStream from opts.context | P2 | ✅ doc-as-divergent |

**Strong-parity confirmation (matched):** lifecycle events, approval-required shape, KILLED error code, drainQueue contract, cost: null honesty, pendingQueue context for streamer/reactor, kill idempotency, socket 0600+secret, orphan-sweep prefix, resetSession, fireUserMessage, bridge-disconnect drain, tool dedup, rate limit, pong watchdog, session-id adoption.

### Resolution batches (parity audit)

- **Batch 1 — P0s:** P4 (--agent) + P8 (--resume) — both small, both load-bearing
- **Batch 2 — P1 telemetry + config:** P1 (db logEvent), P2 (queueCap), P3 (display hint), P5 (--effort), P7 (getTopicConfig precedence), P6 (--setting-sources), P14 (queue-drop), P15 (turn-timeout)
- **Batch 3 — P2 polish:** P17 (init payload casing), P18/P23/P25/P26 (doc-as-divergent), P19 (_killing flag), P21/P22 (error codes), P20 (drainQueue cleanup)
- **Batch 4 — Divergence doc updates:** P11, P12, P13, P16, P18, P23, P25, P26 all need entries

## HeartbeatReactor wiring follow-up — 2026-05-25

Pre-wire investigation surfaced three event-emit gaps that would have stalled the cycling-emoji indefinitely on non-`_resolveTurn` exit paths:

| # | Where | Gap | Status |
|---|---|---|---|
| HR1 | `channels-process.js:830-849` (hardTimer) | `turn-timeout` rejected pending without `idle` emit | ✅ `47aa1c1` |
| HR2 | `channels-process.js:resetSession` | rejected pendings + `session-reset` without `idle` emit | ✅ `47aa1c1` |
| HR3 | `channels-process.js:interrupt grace-resolve` | synthetic `pending.resolve({...,resultSubtype:'interrupted'})` without `idle` emit | ✅ `47aa1c1` |
| HR4 | `polygram.js:handleMessage` | HeartbeatReactor never instantiated (purely additive UX deferred since Phase 1) | ✅ `cf3448a` |
| HR5 | `heartbeat-reactor.js:_bind` | Only `close` stopped permanently; few-ms visible gap before `bridge-disconnected → kill → close` cleared the emoji | ✅ `cf3448a` (added `bridge-disconnected` to stop triggers) |
| HR6 | `polygram.js:51` (pre-existing) | Duplicate `chunkMarkdownText` const from `9047752` — `node polygram.js` failed at boot. Tests never caught it because they import lib/ modules, not the entry point. | ✅ `cf3448a` drive-by |

**Architecture choice (with reasoning):** lifecycle owned by `polygram.js` (alongside the existing symbolic `reactor` at `:1003`), not inside `ChannelsProcess`. Rejected the per-`send()` instantiation inside the Process because the three HR1/HR2/HR3 gaps would have required cross-cut teardown everywhere; polygram's `handleMessage` already has a single `finally` that catches success/throw/abort/timeout in one place. Rejected Option C (`reaction-hint` event) because it just renames the same "decide when to fire" question. Investigation report at the time of decision was the prompt-pack delivered against `cf3448a` parent.
