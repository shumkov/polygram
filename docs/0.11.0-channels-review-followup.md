# 0.11.0 — Channels driver review follow-up

Tracking the findings from the 2026-05-24 multi-agent ce-code-review pass and their resolution status.

**Review scope:** 10 reviewers (correctness, security, reliability, api-contract, adversarial, testing, maintainability, project-standards, agent-native, learnings-researcher) against the channels work since `0.10.0-spike`. ~70 raw findings → 19 actionable after dedup + anchor-75 gate.

**Run ID:** `20260524-205934-c35ff84c` (per-agent JSON artifacts at `/tmp/compound-engineering/ce-code-review/20260524-205934-c35ff84c/`).

## Status legend

- ✅ Fixed in this branch
- 🟡 Partially fixed (subset landed)
- ⏳ In progress
- ⬜ Not started

## P0 — must fix before production rollout

| # | Title | File | Status | Commit |
|---|---|---|---|---|
| 1 | Bridge secret leak via `--mcp-config` argv (`POLYGRAM_SOCK_SECRET` + `POLYGRAM_SESSION_KEY` visible in `ps`/`/proc/cmdline`) | `lib/process/channels-process.js:193` | ✅ | mcp-config written to 0o600 tmp file; argv carries file path only |
| 2 | File exfiltration via `reply.files` — prompt-injected Claude uploads `/etc/passwd`, SSH keys, AWS creds to TG | `lib/process/channels-tool-dispatcher.js:87` | ✅ | `validateAttachmentPath` + per-session allowlist + realpath; symlinks to outside rejected |
| 3 | `bridge-disconnected` event has zero consumers — daemon-respawn premise unimplemented | `lib/process/channels-process.js:310-319` | ✅ | ProcessManager listens → kills dead instance → frees LRU slot → lazy respawn via getOrSpawn on next msg (with persisted claudeSessionId for `claude --resume`) |

## P1 — must fix before Phase 1 default-flip

| # | Title | File | Status | Commit |
|---|---|---|---|---|
| 4 | Reply fan-out across pending turns — bridge `tool` msg carries no `turn_id` (user_msg does) | `lib/process/channels-process.js:413-420` + bridge protocol | ✅ | reply tool schema now requires `turn_id`; `_recordReplyForPendingTurn` routes by it; fallback for single-pending logged |
| 5 | hardTimer leak on bridge-disconnect (10-min zombie timers) | `channels-process.js:310-319` | ✅ | `c14f112` |
| 6 | Daemon-side ping loop never verifies pongs return — half-open socket invisible | `channels-process.js:370-372, 601-606` | ✅ | `lastPongAt` recorded on each `pong`; `pongWatchdog` fires every 5s; declares bridge dead at 30s |
| 7 | `tool_ack` 30s timeout returns "delivery failed" to Claude → may retry → duplicate TG msgs | `channels-bridge.mjs` | ✅ | daemon-side `recentToolCallIds` FIFO cache; duplicate `tool_call_id` re-ACKs without re-dispatch |
| 8 | `respondToPermission` blindly writes verdicts — double-fire writes two | `channels-process.js:363` | ✅ | `c14f112` (Set-based dedup) |
| 9 | Socket file permissions TOCTOU race (listen → chmod window) | `channels-process.js:152` | ✅ | `process.umask(0o077)` wraps `listen()`; restored immediately after |
| 10 | Inbound meta values (user/chat_id/msg_id/turn_id) not XML-escaped before `<channel ...>` attributes | `channels-bridge.mjs:135` | ✅ | `c14f112` (`escapeChannelAttr`) |
| 11 | Channels tmux name lacks `polygram-` prefix → orphan-sweep blind → leaks per restart | `channels-process.js:134` | ✅ | `c14f112` |
| 12 | Quiet-window heuristic resets forever when Claude streams progress replies → 10min hang | `channels-process.js:417` | ✅ | `maxRepliesPerTurn` cap (default 20); resolves immediately on cap hit |
| 13 | `approval-required.toolInput` is object `{description, input_preview}` but TmuxProcess emits string; `normalizeTuiToolInput` coerces non-string → `''` → silent empty approval card | `channels-process.js:352-356` | ✅ | `_formatToolInputForApproval` returns string; folds description into preview when distinct |
| 14 | `pendingQueue` never populated → no Telegram live-edit, reactor heartbeats, subagent announce on channels chats | `channels-process.js:454-486` | ✅ | `send()` pushes `{turnId, context}` to `pendingQueue`; `_resolveTurn` + drain paths pop it |
| 15 | Missing canonical events: `stream-chunk`, `autonomous-assistant-message`, `compact-boundary`, `tool-use`, `phase-change` | `channels-process.js:141-553` | 🟡 | `tool-use` + `autonomous-assistant-message` emitted; `stream-chunk`/`compact-boundary`/`phase-change` deferred (channels protocol doesn't expose) |
| 16 | Reply text recorded even when `toolDispatcher` failed | `channels-process.js:408` | ✅ | `c14f112` |
| 17 | `HeartbeatReactor.inStall` never cleared on liveness → 🥱 frozen after first stall | `heartbeat-reactor.js:179` | ✅ | `c14f112` |
| 18 | `_handleStartupDialogs` trust/dev-channel/timeout branches untested | `channels-process.js:215-253` | ✅ | 3 new integration tests cover dev-channel Enter, trust-dialog Enter, timeout-throw. Bonus: caught + fixed a race in `_waitForBridgeHandshake` (early bridge-ready event was lost). |
| 19 | `channels-bridge.mjs` has zero direct tests | `channels-bridge.mjs:1-246` | ✅ | New `tests/channels-bridge.test.js` spawns the real bridge subprocess against a fake daemon socket. 6 tests: env validation, hello+session_init, stdin EOF clean exit, hello_reject exit code, ping→pong, hostile-input safety. |

## P2 — should address (15 condensed)

| Category | Items | Status |
|---|---|---|
| Cleanup leaks on startup paths | R6+R7+R10 (3 throw paths leak tmux+socket) | ✅ `c14f112` (try/catch in start) |
| respond closure drops deny-with-feedback message | AC2 | ⬜ accepted — Channels protocol doesn't carry feedback string |
| `pickBackend` silent typo fall-through | AC3 | ✅ `c14f112` |
| cost=0 + zero metrics propagate to billing | AC4 | ✅ PmSendResult now uses `null` to mean unmeasured-subscription |
| C28 fireUserMessage skip breaks `/compact` silent-retry | AC7 | ✅ implemented `fireUserMessage` (writes user_msg w/o pending-turn) |
| C13 resetSession skip breaks `/new`+`/reset` | AC8 | ✅ implemented `resetSession` (drains pendings, clears claudeSessionId, emits session-reset) |
| Stale ping resets watchdog | R5 | ✅ bridge only updates `lastPing` on actual `ping`, not any inbound data |
| Partial file-attach silent ok:true | R9 | ✅ rolled into P0 #2 — dispatcher returns ok:false with details on failed attaches |
| ChannelsProcess 600+ LoC owns 5 concerns | M1 | ⬜ refactor target (extract `ChannelsBridgeServer` + `TmuxStartupGate`) |
| `resolveClaudeBin` dead helper | M2 | ✅ `c14f112` |
| `deliverReplies` two code paths | M3 | ✅ required at construction; no lazy fallback |
| No `pendingPerms` map (daemon restart loses) | ADV-12 | ⬜ same-root as P0 #3 lazy-respawn — daemon-side state for open perms not in scope |
| No rate limit on reply tool calls | ADV-6 | ✅ token-bucket (5/s, burst 20) on `_dispatchToolCall` |
| `chunkTextPlain` splits HTML mid-tag | ADV-14 | ✅ polygram.js uses `chunkMarkdownText` (fence-aware) for channels driver |
| Outbound reply.text not escaped (defense-in-depth) | ADV-10 | ⬜ accepted — no real attack vector inside daemon process |
| `claudeSessionId` adopt visible to no upstream cache | ADV-8 | ⬜ `session-id-refreshed` event already emitted; DB persistence listener is polygram.js wiring (deferred) |
| Bridge protocol no zod schemas | M4 | ⬜ refactor target |
| Magic numbers scattered | M5 | 🟡 named most ping/watchdog/burst constants in P1 batch; full audit deferred |

## P3 — narrow scope

- Reaction whitelist 🤖 may not be in Bot API allowlist — wrapped in try/catch, advisory — ✅ accepted (already swallow on error)
- `--dangerously-load-development-channels` kill-switch missing — ✅ documented in risk register
- `chat_id`-mismatch log no rate limit — ✅ 1s throttle per session
- `/tmp` socket files survive `kill -9` — 🟡 partial via orphan-sweep (channels prefix landed in P1 #11); full sweep planned in daemon-wiring round
- HeartbeatReactor stop() races in-flight setReaction — ✅ monotonic `_reactionToken`
- `interrupt()` doesn't clear pending turn timers — ✅ 5s grace window → `resultSubtype: 'interrupted'` resolution
- Naming drift `claudeSessionId` / `CLAUDE_SID` — ✅ bridge renamed `CLAUDE_SID` → `CLAUDE_SESSION_ID`
- `phase0-bridge.mjs` spike code orphaned — ✅ deleted `scripts/channels-spike/`
- `simulateCompactBoundary` throws instead of returning sentinel — ✅ returns `{supported:false, reason}`
- Test fake-runner duplicated (M6) — ⬜ accepted; channels runner shape differs from tmux runner, full dedup low-value
- `polygram.js` wiring inline (watch on backend #4) — ⬜ watch-item, no action

## Cross-backend asymmetry — `docs/0.10.0-backend-divergence.md` entries needed

From agent-native review. Three channels-skipped scenarios with user-visible divergence:
- **C24 autonomous-assistant-message** → `ScheduleWakeup` push silently drops on channels
- **C10/C11/C23 injectUserMessage** → autosteer + edit-correction unreachable on channels
- **C13/C15/C28** → `/new`, `/model`, `/effort`, `/compact` lose agent-side wiring

## Learnings to honor (from `ce-learnings-researcher`)

- **rc.32 STALL=45s** calibrated against shumorobot Music traffic — HeartbeatReactor reuses correctly
- **rc.39/rc.45 sanitize-reply** — verify `lib/telegram/sanitize-reply.js` runs on channels-tool-dispatcher path
- **S1 spawn-leak pattern** — `ChannelsProcess.start` now has try/catch teardown per `c14f112`
- **rc.33 onInit topic-config resolution** — verify per-topic `pm:'channels'` override honored
- **lib/ipc/server.js** is the canonical mirror pattern (stale-socket unlink + 0600 + timingSafeEqual)

## Resolution rollout plan

Working through tiers in order: **P0 → P1 → P2 → P3**.

- Phase 0 (now): P0 #1, #2, #3 (the daemon-respawn half remaining)
- Phase 1 (next): P1 items #4, #6, #7, #9, #13, #14, #15
- Phase 2: P1 test gaps #18, #19
- Phase 3: P2 batch
- Phase 4: P3 + cross-backend divergence doc

Each commit updates this doc's status column.
