# 0.12.0 — File send/receive (channels + IPC)

## What this fixes

File-send **never worked** on this codebase. Root cause: callers passed
grammy a Telegraf-style `{ source: '/abs/path' }` envelope as the file
param. grammy 1.x doesn't recognize that shape, so it serialized the
object and Telegram tried to read it as a URL → every upload failed with
`400 Bad Request: invalid file HTTP URL specified: Wrong port number`.
This hit both the channels `reply(files=[...])` path and the IPC send
path (same error in the logs). The existing dispatcher test used a fake
`send` and only asserted the *method*, so it never caught the bad shape.

Surfaced live in the shumorobot Music topic (2026-05-31): claude tried to
send tracks via `reply(files)`, hit the upload bug + an allowlist gap +
the size cap, and flailed across `/Volumes/…`, `~/Downloads`, `/tmp`, and
the IPC URL path — all failing.

## The fixes

1. **Upload coercion** (`lib/telegram/input-file.js`) — at the single send
   choke point (`tg()` → `send()` in `lib/telegram/api.js`),
   `coerceFileParams(method, params)` turns a `{ source: path }` file param
   into a real grammy `new InputFile(path)`. No-op for file_id / URL
   strings, existing InputFile instances, Buffers. Fixes channels **and**
   IPC at once.

2. **Staging dir** (`lib/process/cli-process.js`) — at spawn, CliProcess
   creates `<tmpdir>/polygram-attachments/<sessionKey>/` (always on the
   dispatcher allowlist, but previously never created → the literal path
   claude guessed didn't exist / didn't realpath-match). Stored as
   `this.attachmentStagingDir` (realpath'd) and surfaced to the system
   prompt. Auto-purged when the session goes idle (`_finalizeTurn`); the
   whole dir is removed on kill. You don't tell claude to delete.

3. **System prompt** — the channels HARD CONTRACT now teaches claude to
   send files via `reply(files=["<staging-dir>/name.flac"])`, NOT via
   Bash/curl/Telegram-API/IPC, and states the 50 MB upload limit.

4. **Caps** (`lib/attachments.js`) — inbound (user → bot) per-file cap
   raised 10 MB → **20 MB** (Telegram cloud's getFile ceiling), total
   20 MB → 50 MB.

## Limits + per-chat caps

The caps are **backend-derived**, not a fixed 20/50 and NOT "adaptive"
(there is no intermediate tier). `resolveFileCaps()` returns:

| Backend | bot → user | user → bot |
|---|---|---|
| Cloud Telegram (default) | **50 MB** | **20 MB** |
| Local Bot API server (`apiRoot` set) | **2 GB** | **2 GB** |

These are Telegram's own hard ceilings — a per-chat override can **lower**
them but never exceed them (Telegram rejects beyond regardless).

Big lossless WAVs (50–64 MB) cannot cross on **cloud** — that's Telegram's
limit, not ours. FLAC/MP3 under the limit work. With the local server they
go fine.

### Per-chat / per-topic override

`config.chats[id].maxFileBytes` (bytes), or per-topic
`topics[t].maxFileBytes`, overrides the default for that chat/topic,
clamped to the backend ceiling:

```jsonc
"chats": {
  "-1003807211164": {
    "maxFileBytes": 104857600,        // 100 MB — only effective with apiRoot set
    "topics": {
      "3": { "name": "Music", "maxFileBytes": 524288000 }  // 500 MB for Music
    }
  }
}
```

Resolution: topic `maxFileBytes` → chat `maxFileBytes` → backend default.
On cloud, an override above 20 MB-in / 50 MB-out is clamped to those (you
can't beat Telegram's cloud limit by config). The outbound cap is enforced
in the dispatcher with a clear "file too large" error BEFORE upload (vs
Telegram's cryptic 413), so claude can convert/compress and retry.

## Optional: 2 GB via a self-hosted Bot API server

To send/receive up to **2 GB** both ways, run the official
`telegram-bot-api` server locally and point polygram at it.

### polygram side (already wired)

Set in `config.json`:
```jsonc
"bot": {
  "apiRoot": "http://localhost:8081"
}
```
Omitted → cloud Telegram, unchanged (this is the only polygram change; it's
additive and reversible). Boot logs `using local Telegram Bot API server`
when set.

### Companion server (separate daemon — set up when ready)

It is a **standalone process**, not part of polygram, and is
**localhost-only** — it makes OUTBOUND connections to Telegram's DCs; nothing
external connects to it. No public port, no webhook, no exposure.

**YOUR 2 manual steps (cannot be automated):**
1. Get `api_id` + `api_hash` from <https://my.telegram.org> →
   "API development tools". (Needs your Telegram phone login — only you
   can do this.)
2. Build the binary (it is NOT on Homebrew):
   ```
   ops/setup-telegram-bot-api.sh
   ```
   (Compiles TDLib + the server; takes several minutes. Installs to
   `/opt/homebrew/bin/telegram-bot-api`.)

**Then (the script prints these too):**
3. Edit `ops/telegram-bot-api.plist.example` — paste your `api_id`/`api_hash`,
   confirm the binary path. `cp` it to
   `~/Library/LaunchAgents/com.telegram.bot-api.plist`.
4. `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.telegram.bot-api.plist`
5. Verify: `curl -s http://localhost:8081/` → a Telegram error JSON (not
   connection-refused) means it's up.
6. Set `config.bot.apiRoot = "http://localhost:8081"`, restart polygram.
   Boot log prints `using local Telegram Bot API server`.

The plist binds to `127.0.0.1:8081` and uses `--local` (enables 2 GB
limits + local-file paths). Provided files:
- `ops/setup-telegram-bot-api.sh` — builds/installs the binary
- `ops/telegram-bot-api.plist.example` — the launchd job template

Operational note: it's another daemon to babysit. If it's down while
`apiRoot` points at it, file calls fail until it's back. Cost is
operational, not code.

## Tests

- `tests/input-file.test.js` — coercion: `{source}`→InputFile, passthrough
  for file_id/URL/InputFile, no-op for non-file methods.
- `tests/file-send-staging.test.js` — staging dir is allowlisted, realpath
  matches, purge-on-idle, no-op safety, finalize auto-purge.
- E2E verified: `tg()` hands `bot.api.raw.sendDocument` a real `InputFile`
  (grammy will multipart-upload) instead of the broken `{source}` object.
