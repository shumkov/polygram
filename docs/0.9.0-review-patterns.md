# 0.9.0 Cleanup — Pattern Review

> Returned by `compound-engineering:review:pattern-recognition-specialist`
> via Agent task on 2026-05-06. The reviewer's system prompt forbade
> writing the file directly; this is the verbatim findings copied from
> its returned text so the review persists for the 2026-05-15
> architecture meeting.

The cleanup achieved its mechanical scope (deletes verified, code paths collapsed, tests green at HEAD) but left a substantial trail of stale text references and a few real dead-code paths and pattern inconsistencies. Findings below by severity.

## CRITICAL — runtime / ship surface

### C1. `package.json` `files` array still ships a deleted directory
`/Users/ivanshumkov/Projects/shumkov/polygram/package.json:14` lists `"bin/"` in the `files` whitelist, but the entire `bin/` directory was deleted in commit 7. `npm pack` will warn (or some npm versions error) on a missing `files` entry. Drop the line.

### C2. `appendDisplayHintCliArgs` is dead code that's still imported and exported
- `/Users/ivanshumkov/Projects/shumkov/polygram/polygram.js:47` imports it.
- `polygram.js` never calls it (verified via `grep "appendDisplayHintCliArgs("`).
- `/Users/ivanshumkov/Projects/shumkov/polygram/lib/telegram-prompt.js:108-111` defines it; the JSDoc literally says "For the CLI pm (`claude -p ...`)…".
- `/Users/ivanshumkov/Projects/shumkov/polygram/tests/telegram-prompt.test.js:196-215` has 4 tests pinning the dead helper.

### C3. `onRespawn` callback is wired into pmOpts but SDK pm never invokes it
- `polygram.js:3971-3987` passes a substantive `onRespawn` callback into `pmOpts`.
- `lib/process-manager-sdk.js:185-225` constructor accepts `onInit / onResult / onClose / onStreamChunk / onCompactBoundary` etc. — `onRespawn` is NOT in the destructured list and is never called anywhere in the file.
- This means the "✓ Using sonnet now" / "✓ Effort is high now" confirmation message after a /model or /effort drain has been **silently broken** since CLI pm was deleted (commit 6). Either wire `onRespawn` into SDK pm at the post-applyFlagSettings/setModel point, or delete the dead callback.

## HIGH — pattern drift / dead branches

### H1. Inconsistent feature-detection on `pm` methods
- **Still feature-detects** (should call directly): `polygram.js:1818, 1821, 2166, 2170` — `pm.applyFlagSettings`, `pm.setModel`.
- **Calls directly** (correct for single-pm world): `polygram.js:2131, 2663, 2740, 3299, 3301`.

### H2. `lib/pm-router.js` is a no-op that adds noise
The 18-line header comment is longer than every consumer's call site. Either delete the file (drop the seam) or compress the header to one line.

### H3. Stale comment claims `buildApprovalKeyboard` exists
`polygram.js:1378` references the 2-button keyboard that was deleted in commit 8.

### H4. README has 4 stale references
- `README.md:331-362` — entire "Approvals" section describes the deleted PreToolUse `bin/approval-hook.js` flow.
- `:352` — sample `settings.json` references `/abs/path/to/polygram/bin/approval-hook.js`.
- `:367` — claims "643 tests, 158 suites" — stale.
- `:378` — layout diagram lists `bin/approval-hook.js`.
- `:380` — layout diagram mentions deleted "process-manager".

### H5. `lib/pm-interface.js` JSDoc still describes the dual-pm world
- `:4-5` — "Both `lib/process-manager.js` (CLI pm) and …" → CLI deleted.
- `:5-6` — describes the now-identity router as "forwards calls based on per-chat policy."
- `:8-12` — explains feature-detection via `pm.pickFor(sessionKey)` → method no longer exists.
- `:73` — "Returns false on CLI pm…" — CLI pm gone.

### H6. `lib/process-manager-sdk.js` header describes CLI-pm coexistence as current
- `:6-9` — "Public API matches `lib/process-manager.js` … Phase 4 deletes the CLI version after Phase 5 soak proves the SDK version stable." — past tense, please.
- `:23-25` — "(same as CLI version's stream-json model)" — gone.
- `:31` — `D3 /effort via applyFlagSettings — DELETE requestRespawn` reads as future TODO; done.

### H7. `lib/error-classify.js` self-describes as 0.7.7 + future tense
- `:4-32` — header dates the file at "0.7.7" with "0.8.0 SDK migration" as future. All shipped.
- `:107-108` — "Phase 2 of 0.8.0 wires `pm.resetSession()` to these" → wired.
- `:120` — "see lib/process-manager.js" — deleted.

### H8. Other lib/ files with stale CLI-pm exposition
- `lib/agent-loader.js:5-11` — header opens with "Background: today's CLI pm passes `--agent <name>` on spawn" — only world now.
- `lib/approval-waiters.js:7` — "callback (replaces today's `bin/approval-hook.js` IPC)" — replaced.
- `lib/telegram-prompt.js:101-103` — JSDoc for the dead `appendDisplayHintCliArgs`.
- `lib/stream-reply.js:214` — "Used by `onAssistantMessageStart` in process-manager.js when…" — refers to deleted file.

## HIGH — `polygram.js` body comments

10+ comments still differentiate "SDK pm vs CLI pm". Now meaningless:
- `:1472`, `:1605`, `:1808-1810`, `:2159-2164`, `:2608-2618`, `:2775-2779`, `:3279-3282`, `:3897`, `:4158-4160`.

## MEDIUM — test surface

### M1. `tests/_helpers/fake-pm.js` still supports `requestRespawn`
- `:40` JSDoc param.
- `:83-88` opt-in branch defines `pm.requestRespawn`.
- `:14-15` example uses dead `cliPm`/`sdkPm`/`pickPmKindFor` router params.

### M2. `tests/pm-interface-contract.test.js` constructs `'cli'`-named fakes
- `:9-10` header: "Real pm classes (process-manager.js / process-manager-sdk.js)…" — only one impl.
- `:26` `makeFakePm('cli')` — naming reflects the deleted world.
- `:32-36` `OPTIONAL_METHODS` array MISSING `injectUserMessage`. Coverage gap.

### M3. `tests/pm-router.test.js` references the deleted dual-pm world
- `:4-8` header explains 0.8.x dual-pm history. Reasonable as historical context.

## LOW — historical-context docs

`docs/0.8.0-architecture-decisions.md`, `docs/0.8.0-sdk-migration-plan.md`, `docs/0.8.0-phase0-findings.md`, `docs/autosteer-contract-fix-research.md`, `docs/polygram-design.md` all reference the deleted machinery. The 0.8.0 plan got a v9 appendix; the rest are pre-cleanup snapshots — leave for archaeology.

## Inconsistency summary

| Pattern | Drift |
|---|---|
| Feature-detect on optional pm methods | 4 sites feature-detect; 5 sites call directly |
| `process-manager.js` references | 5 lib/ files + 2 polygram.js sites + 1 test file |
| "CLI pm" / "SDK pm" contrasts | 10+ inline comments in polygram.js, 8+ in lib/ |
| Test fake naming | `'cli'` strings persist in `fake-pm.js` JSDoc + contract test |

## Top recommendations (priority order)

1. Fix C1 (package.json `files`) and C3 (onRespawn dead callback / broken UX) — runtime impact.
2. Delete C2 (`appendDisplayHintCliArgs` + tests).
3. Resolve H1: pick one calling convention for pm methods, apply globally.
4. Sweep H4-H8 stale comments. The pm-interface.js JSDoc (H5) is highest leverage.
5. Clean up M1-M2 test scaffolding.
6. Consider whether pm-router.js (H2) earns its keep.
